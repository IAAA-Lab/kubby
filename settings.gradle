rootProject.name = 'kubby'

enableFeaturePreview('GRADLE_METADATA')

def module(group, name) {
    include(name)
    project(":$name").projectDir = file("$group/$name")
}


include ':kubby-ext'

module('kubby-samples/app', 'pubby')
