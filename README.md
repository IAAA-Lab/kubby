
```kotlin
fun Application.main() {
    // Obtains the kubby configuration from the application.conf file.
    val kubbyConfig = environment.config.config("kubby")
    val 
}
```


```
docker run -d --name sonarqube -p 9000:9000 -p 9092:9092 -f docker/sonar-kotlin/Dockerfile
```
